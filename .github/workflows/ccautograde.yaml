name: CC Autograde

on:
  push:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 11
      uses: actions/setup-java@v1
      with:
        java-version: 11
    - name: Build with Maven
      run: mvn clean test
      env:
        AID: "5710353417633792"
        CID: "b4db91dd-3a75-4a77-8f44-152cabc3fe1e"
                                    
